import { render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import { PassportList } from './passport-list';
describe('PassportList', () => { it('shows readable application records without internal codes or work queues', () => { render(<PassportList initial={[{ id: 'case', caseCode: 'FP-1', programName: '軟體補助申請', year: 2026, state: 'approved', submittedAt: '2026-01-01T00:00:00.000Z', unresolvedTaskCount: 1, securityAlert: true }]} />); expect(screen.getByRole('heading', { name: '2026 年' })).toBeInTheDocument(); expect(screen.getByRole('link', { name: /軟體補助申請/ })).toBeInTheDocument(); expect(screen.getByText('審核通過')).toBeInTheDocument(); expect(screen.getByText('2026年1月1日送出')).toBeInTheDocument(); expect(screen.queryByText(/FP-1|approved|待辦|資安提醒|示範/)).not.toBeInTheDocument(); }); });
