import React, { useState } from 'react'; import { createRoot } from 'react-dom/client';
function AdminShell() { const [loggedIn, setLoggedIn] = useState(false); const [name, setName] = useState(''); const [password, setPassword] = useState(''); const [message, setMessage] = useState(''); async function login(event: React.FormEvent) { event.preventDefault(); const response = await fetch('/admin/v1/sessions', { method: 'POST', headers: { 'content-type': 'application/json', origin: window.location.origin }, body: JSON.stringify({ displayName: name, password }) }); if (response.ok) { setLoggedIn(true); setMessage('已登入本機管理後台。'); } else setMessage('登入失敗，請確認帳號或密碼。'); } if (!loggedIn) return <main><h1>FlowPass 管理後台</h1><p>只允許在本機開啟。</p><form onSubmit={login}><label>管理員帳號<input value={name} onChange={(event) => setName(event.target.value)} required /></label><label>密碼<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} required /></label><button type="submit">登入</button></form><p role="status">{message}</p></main>; return <main><h1>案件審查</h1><nav><a href="#cases">案件佇列</a> · <a href="#audit">稽核紀錄</a></nav><p role="status">{message}</p><section id="cases"><h2>案件佇列</h2><p>案件資料只會在本機登入後載入。</p></section><section id="audit"><h2>稽核紀錄</h2><p>敏感欄位不在此頁公開。</p></section></main>; }
createRoot(document.getElementById('root')!).render(<AdminShell />);
