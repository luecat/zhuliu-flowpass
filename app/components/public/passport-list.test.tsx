import { render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import { PassportList } from './passport-list';
describe('PassportList', () => { it('groups records by year and shows safe summaries', () => { render(<PassportList initial={[{ id: 'case', caseCode: 'FP-1', programName: 'Demo', year: 2026, state: 'approved', submittedAt: '2026-01-01', unresolvedTaskCount: 1, securityAlert: true }]} />); expect(screen.getByText('2026')).toBeInTheDocument(); expect(screen.getByText(/待辦 1/)).toBeInTheDocument(); expect(screen.getByText(/資安提醒/)).toBeInTheDocument(); }); });
