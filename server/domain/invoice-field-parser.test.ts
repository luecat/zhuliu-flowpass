import { describe, expect, it } from 'vitest';
import { parseInvoiceFields } from './invoice-field-parser';
describe('invoice field parser', () => { it('normalizes ROC date, amount and currency with provenance', () => { const result = parseInvoiceFields([{ page: 2, text: '發票號碼：AB12345678 民國113年5月6日 NT$ 1,234 TWD', confidence: 0.9, boundingBox: { x: 1, y: 2, width: 3, height: 4 } }]); expect(result.find((x) => x.fieldName === 'invoice_number')?.normalizedValue).toBe('AB12345678'); expect(result.find((x) => x.fieldName === 'invoice_at')?.normalizedValue).toContain('2024-05-06'); expect(result.find((x) => x.fieldName === 'amount_minor')?.normalizedValue).toBe('123400'); expect(result[0].sourcePage).toBe(2); }); });
it('parses USD minor units and marks impossible calendar dates for review', () => { const usd = parseInvoiceFields([{ page: 1, text: 'Invoice No: AB123456 USD 1,234.56 2026/02/03', confidence: 0.9, boundingBox: { x: 0, y: 0, width: 1, height: 1 } }]); expect(usd.find((x) => x.fieldName === 'amount_minor')?.normalizedValue).toBe('123456'); expect(usd.find((x) => x.fieldName === 'currency')?.normalizedValue).toBe('USD'); const invalid = parseInvoiceFields([{ page: 1, text: '發票日期 2026/02/30', confidence: 0.9, boundingBox: { x: 0, y: 0, width: 1, height: 1 } }]); expect(invalid.find((x) => x.fieldName === 'invoice_at')?.normalizedValue).toBeNull(); });
